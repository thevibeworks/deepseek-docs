name: fetch

on:
  schedule:
    - cron: "17 */6 * * *"   # every 6 hours
  workflow_dispatch:

concurrency:
  group: fetch
  cancel-in-progress: false

permissions:
  contents: write
  pull-requests: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v5

      # dsh docs come from github/npm, not api-docs; fetched first so the
      # index rebuild in the main fetcher sees fresh dsh content.
      - name: Fetch dsh docs (deepseek-harness + npm)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: uv run scripts/dsh.py

      - name: Fetch docs
        run: uv run scripts/fetcher.py

      # Two different questions. "Did the tree move?" decides whether there
      # is anything to commit. "Is there news?" decides whether to spend an
      # agent call and wake a phone -- and those came apart badly: 19 of the
      # last 30 commits here changed nothing but content/.metadata.json,
      # where a page's entry flips between a title and a transient "served
      # fallback shell" error as upstream wobbles. Every one of those flips
      # ran triage, committed, and pushed "DeepSeek docs updated" to Eric's
      # phone for zero documentation changes. A notification channel that
      # cries wolf 19 times in 30 is not a notification channel.
      - name: Detect changes
        id: diff
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if [ -z "$(git status --porcelain)" ]; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "newsworthy=false" >> "$GITHUB_OUTPUT"
            echo "Mirror is up to date."
          elif [ -z "$(git status --porcelain -- ':!content/.metadata.json')" ]; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "newsworthy=false" >> "$GITHUB_OUTPUT"
            echo "Bookkeeping only: fetch-state metadata moved, no content changed."
            git status --short | head -30
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "newsworthy=true" >> "$GITHUB_OUTPUT"
            git status --short | head -30
          fi

      # The agent only DECIDES: it reads the diff and writes gitignored
      # .decision/ files. It runs with permissions skipped, which is safe
      # because its blast radius is nil -- every side effect (commit, push,
      # PR) lives in the deterministic Publish step below. Runs Claude Code
      # on the DeepSeek API via the Anthropic-compatible endpoint.
      - name: Agent triage (deepseek-v4-flash)
        if: steps.diff.outputs.newsworthy == 'true'
        uses: anthropics/claude-code-action@v1.0.168
        env:
          ANTHROPIC_BASE_URL: https://api.deepseek.com/anthropic
          ANTHROPIC_MODEL: deepseek-v4-flash
          ANTHROPIC_DEFAULT_OPUS_MODEL: deepseek-v4-flash
          ANTHROPIC_DEFAULT_SONNET_MODEL: deepseek-v4-flash
          ANTHROPIC_DEFAULT_HAIKU_MODEL: deepseek-v4-flash
          ANTHROPIC_SMALL_FAST_MODEL: deepseek-v4-flash
          CLAUDE_CODE_SUBAGENT_MODEL: deepseek-v4-flash
        with:
          anthropic_api_key: ${{ secrets.DEEPSEEK_API_KEY }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          show_full_output: true
          claude_args: |
            --model deepseek-v4-flash
            --dangerously-skip-permissions
          prompt: |
            You are the docs editor for this DeepSeek API docs mirror. The
            fetcher already ran; the working tree holds whatever changed
            upstream. You DECIDE what the changes mean. You do NOT commit,
            push, or open PRs -- a deterministic publish step consumes the
            decision files you write.

            ## Workflow

            1. `git status` and `git diff` to understand what changed.
            2. Classify the change and write the decision files.

            ## Signal criteria

            **high** (humans should look -- a PR stays open for review):
            - New or changed model versions, pricing, context/output limits
              (quick_start/pricing.md)
            - New API endpoints, parameters, or breaking schema changes (api/)
            - New guides or new model releases (guides/, news/)
            - Changed base URLs, auth, or rate-limit policy
            - dsh breaking changes (content/en/dsh/): deleted or renamed
              pages, plugin manifest/API format changes, new npm releases
              (dsh/npm.md) -- dsh is a developer preview that breaks
              things without migration paths

            **minor** (archive freshness -- committed straight to main):
            - Wording, typos, formatting, link fixes
            - Metadata or index churn (.metadata.json, llms.txt, llms-full.txt)
            - zh-cn-only changes that mirror an already-known en change
            - dsh doc wording/typo churn that changes no interface

            ## Decision files (your only output)

            Write into `.decision/` (gitignored):
            - `.decision/signal` -- exactly `high` or `minor`
            - `.decision/title` -- one line, no "docs:" prefix, name the
              actual change (e.g. `deepseek-v4-pro effort mapping updated`)
            - `.decision/body.md` -- short report: one paragraph on WHY it
              matters, then a bullet list of highlights, most interesting
              first.

            Focus on why changes matter to developers using the DeepSeek
            API, not on file statistics.

      # Deterministic publisher: all side effects in plain bash.
      - name: Publish
        if: steps.diff.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          BARK_SERVER: ${{ secrets.BARK_SERVER }}
          BARK_KEY: ${{ secrets.BARK_KEY }}
          NEWSWORTHY: ${{ steps.diff.outputs.newsworthy }}
        run: |
          set -euo pipefail
          D=.decision

          # iPhone push via Bark (same pattern as claude-code-docs). Changed
          # pages are captured from the dirty tree before anything commits.
          PAGES=$(git status --porcelain content/ | awk '{print $NF}' | head -12)
          notify() {  # $1 = link target (commit or PR)
            # Bookkeeping commits get no push. The mirror stays honest in
            # git; the phone only rings when a document actually changed.
            [ "${NEWSWORTHY:-true}" = "true" ] || return 0
            [ -n "${BARK_SERVER:-}" ] && [ -n "${BARK_KEY:-}" ] || return 0
            jq -n --arg k "$BARK_KEY" --arg t "DeepSeek docs updated" \
              --arg b "${TITLE}"$'\n\n'"${PAGES}" --arg u "$1" \
              '{device_key:$k, title:$t, body:$b, url:$u, group:"DeepSeek Docs"}' \
            | curl -sS -X POST "$BARK_SERVER/push" -H 'Content-Type: application/json' -d @- \
            || echo "::warning::bark notification failed (sync itself is fine)"
          }
          # No triage ran for a metadata-only run, so do not go looking for
          # a decision and do not warn about its absence -- that is the
          # expected path now, not a degraded one.
          if [ "${NEWSWORTHY:-true}" != "true" ]; then
            git add -A
            git commit -q -m "chore: refresh upstream fetch-state metadata" \
              -m "No document content changed; content/.metadata.json only."
            git push -q
            echo "Bookkeeping commit pushed; no triage, no notification."
            exit 0
          fi

          SIGNAL=$(tr -d '[:space:]' < "$D/signal" 2>/dev/null || echo "")
          TITLE=$(head -1 "$D/title" 2>/dev/null || echo "")
          if [ -z "$SIGNAL" ] || [ -z "$TITLE" ]; then
            echo "::warning::Agent left no usable decision; committing generic sync so the mirror stays fresh"
            SIGNAL=minor
            TITLE="sync from api-docs.deepseek.com ($(date -u +%F))"
            printf 'Automated sync; agent triage unavailable this run.\n' > /tmp/body.md
          else
            cp "$D/body.md" /tmp/body.md 2>/dev/null || printf '%s\n' "$TITLE" > /tmp/body.md
          fi
          case "$SIGNAL" in high|minor) ;; *)
            echo "::warning::Invalid signal '$SIGNAL', downgrading to minor"; SIGNAL=minor;;
          esac

          # Pricing tripwire (deterministic, overrides the agent): upstream
          # dropped the peak/off-peak plan 2026-08-08 with an undated
          # repricing pending. The moment time-of-day tier language touches
          # pricing.md, downstream consumers need a loud PR the same day.
          # grep -c (not -q) so the pipe drains; -q's early exit SIGPIPEs
          # git diff and pipefail would eat the hit.
          TRIP=$(git diff -U0 -- content/en/quick_start/pricing.md content/zh-cn/quick_start/pricing.md \
            | grep -E '^[+-]' | grep -vE '^(\+\+\+ |--- )' \
            | grep -icE '时段|off-peak|peak|discount|multiplier|分时' || true)
          if [ "${TRIP:-0}" -gt 0 ]; then
            echo "::warning::pricing tripwire hit (${TRIP} changed lines) -- forcing high signal"
            SIGNAL=high
            TITLE="PRICING TRIPWIRE: ${TITLE}"
          fi

          if [ "$SIGNAL" = "minor" ]; then
            git add -A
            git commit -m "docs: ${TITLE}" -m "$(cat /tmp/body.md)"
            git push
            notify "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/commit/$(git rev-parse HEAD)"
            echo "Minor change committed to main."
          else
            # One sync PR at a time, on a fixed branch rebuilt from the main
            # we just checked out. A high-signal change that nobody merges is
            # still there on the next run six hours later; a timestamped
            # branch per run turned that into a new PR every six hours (seven
            # of them for one pricing note, #2..#8). Rebuilding one branch
            # keeps the PR as "main plus everything upstream changed that we
            # have not taken yet" -- reviewing the newest is reviewing all.
            BRANCH="docs/sync"
            git checkout -B "$BRANCH"
            git add -A
            git commit -m "docs: ${TITLE}" -m "$(cat /tmp/body.md)"
            git push -f -u origin "$BRANCH"
            PR=$(gh pr list --head "$BRANCH" --state open --json number --jq '.[0].number // empty')
            if [ -n "$PR" ]; then
              gh pr edit "$PR" --title "docs: ${TITLE}" --body-file /tmp/body.md
              notify "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/pull/${PR}"
              echo "High-signal change, updated open PR #${PR}."
            else
              PR_URL=$(gh pr create --title "docs: ${TITLE}" --body-file /tmp/body.md)
              notify "$PR_URL"
              echo "High-signal change, PR opened: $PR_URL"
            fi
          fi

      # A dirty tree after publish means something silently failed.
      - name: Verify outcome
        if: steps.diff.outputs.changed == 'true'
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            echo "::error::Uncommitted changes remain after publish"
            git status --short | head -20
            exit 1
          fi

      # The question a green pipeline and a fresh main cannot answer: not
      # "is the mirror updating?" but "is anything in it no longer being
      # fetched?" Aggregate freshness hides a single page that stopped
      # coming back, because the other 273 keep moving. Runs every time,
      # including on no-change runs, since a page going dead does not
      # announce itself with a diff.
      - name: Sources that stopped fetching
        if: success()
        run: |
          out=$(python3 scripts/stale_sources.py) || true
          if [ -n "$out" ]; then
            echo "::warning::sources unfetched for 7+ days -- the mirror is holding fossils"
            printf '%s\n' "$out"
            { echo "### Sources that stopped fetching"; echo '```'; printf '%s\n' "$out"; echo '```'; } \
              >> "$GITHUB_STEP_SUMMARY"
          else
            echo "every source fetched within the last 7 days"
          fi

      # Same pattern as claude-code-docs: a failed sync run pushes to the
      # phone instead of rotting silently in the Actions tab.
      - name: Alert on failure
        if: failure()
        env:
          BARK_SERVER: ${{ secrets.BARK_SERVER }}
          BARK_KEY: ${{ secrets.BARK_KEY }}
        run: |
          [ -n "${BARK_SERVER:-}" ] && [ -n "${BARK_KEY:-}" ] || exit 0
          RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          jq -n --arg k "$BARK_KEY" --arg u "$RUN_URL" \
            '{device_key:$k, title:"DeepSeek docs sync failed", body:"needs human", url:$u, group:"DeepSeek Docs"}' \
          | curl -sS -X POST "$BARK_SERVER/push" -H 'Content-Type: application/json' -d @- || true
